# Instalacion en DEV

1. Clonar el repositorio
2. Instalar dependencias ```npm install```
3. Crear archivo .env en la raiz del proyecto basado en .env.template
4. Ejecutar la aplicacion ```npm run start:dev```